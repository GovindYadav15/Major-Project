NODE_ENV=development
PORT=5000

# Comma-separated allowed origins. Use * during early local development only.
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# Optional. If set, ESP32/app requests must include this value in x-api-key.
API_KEY=change-this-secret
